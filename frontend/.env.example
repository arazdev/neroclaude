# NEROCLAUDE Dashboard — Environment Variables
# Set these in Vercel project settings (Settings → Environment Variables)

# Your Oracle Cloud API URL (e.g., http://<oracle-ip>:8080)
NEXT_PUBLIC_API_URL=http://localhost:8080

# The API_SECRET you set in the bot's .env
NEXT_PUBLIC_API_SECRET=
